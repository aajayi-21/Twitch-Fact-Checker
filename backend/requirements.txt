fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
faster-whisper>=1.0
# llm_gemini.py uses the Interactions API (client.aio.interactions) as
# shipped in the 2.x line; older 1.x SDKs import fine but fail at runtime.
google-genai>=2.12
# llm_openrouter.py talks to OpenRouter through the OpenAI SDK pointed at
# their base URL (OpenRouter's officially documented integration path).
openai>=1.90
numpy>=1.26
rapidfuzz>=3.9
websockets>=12.0
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
black>=24.4
